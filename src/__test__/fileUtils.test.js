// 处理路由
// utils.fsFinder(paths[0]);

// 处理视图页
// const result = utils.fsFinderSync(paths[1]);

// utils.fsFinder(paths[1], (filename) => {
//   console.log("🚀 ~ file: generateProjectCode.js:56 ~ utils.fsFinderSync ~ filename:", filename)
// })
// console.log("🚀 ~ file: clearExtraCode.js:55 ~ result:", result, result.length)
// await utils.fsCopyPromise("../src/router/stock.js", "../src/stock.js")
// utils.fsCopy("../src/router/stock.js", "../src/stock.js")
// utils.fsCopySync("../src/router/stock.js", "../src/stock.js")
// utils.fsCopyDirSync("../src/router", "temp")
// utils.fsRenameSync("../src/router/stock.js", "temp/a.js")
// utils.fsRenameSync("../src/router/stock.js", "temp/b.js")
// utils.fsRename("../src/router/stock.js", "temp/a.js",() => {
// })
// utils.fsDeleteSync("temp/a.js")
// utils.fsDeleteDirSync("temp")
// utils.fsMoveSync("../src/router/stock.js", "temp/a.js")
// utils.fsMoveDirSync('tmp', '../src/router/a');
// utils.fsMoveDirSync("../src/router/a", "tmp")
describe('fileUtils suit test', function() {
    it('fsCopy function test, copy a file', () => {
        // expect(utilData.getRealType(arrNums)).toBe('array');
        // expect(utilData.isNumber(0)).toBeTruthy();
        // expect(utilData.isBoolean(false)).toBeTruthy();
        // expect(utilData.isFunction(Function)).toBeTruthy();
        // expect(utilData.isNull(null)).toBeTruthy();
        // expect(utilData.isUndefined(undefined)).toBeTruthy();
        // expect(utilData.isArray([])).toBeTruthy();
        // expect(utilData.isDate(new Date())).toBeTruthy();
        // expect(utilData.isRegExp(/reg/)).toBeTruthy();
        // expect(utilData.isError(new Error())).toBeTruthy();
        // expect(utilData.isPromise(Promise.resolve())).toBeTruthy();
        // expect(utilData.isSet(new Set())).toBeTruthy();
        // expect(utilData.isMap(new Map())).toBeTruthy();
        // expect(utilData.isSymbol(Symbol('test'))).toBeTruthy();
        // expect(utilData.isPlainObject({})).toBeTruthy();
        // expect(utilData.isObject(HTMLElement)).toBeTruthy();
        // expect(utilData.isElement(document.createElement('div'))).toBeTruthy();
        // expect(utilData.isElement(document)).toBeFalsy();
        // expect(utilData.isElement({})).toBeFalsy();
        // expect(utilData.isElement(Function)).toBeFalsy();
    });
});
